services:
  - type: web

    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.mjs
    envVars:
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false # User must set this in Render UI
      - key: healthCheckPath
        value: /health
