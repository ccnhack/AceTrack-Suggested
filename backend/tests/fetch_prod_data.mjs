import dotenv from 'dotenv';
dotenv.config();
import fetch from 'node-fetch';

async function run() {
  console.log("Looking up support users locally first to find an identifier...");
  // Try to find a support user to login with, or just log into production if we know one.
}
run();
