const mongoose = require('mongoose');
mongoose.connect('mongodb+srv://admin:admin@cluster0.abc.mongodb.net/test?retryWrites=true&w=majority', { useNewUrlParser: true });
// I can't do this easily. I will just check the mock users or the Player schema again.
